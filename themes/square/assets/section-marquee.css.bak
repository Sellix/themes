.swiper-container {
  width: 100vw;
  position: relative;
  overflow: hidden;
}

.marquee-section {
  border-top: 1px solid var(--borderColor);
  border-bottom: 1px solid var(--borderColor);
  padding-top: 36px;
  padding-bottom: 36px;
}

/* Effetto animazione inversa per swiper-bottom */
.swiper-container.swiper--bottom::after {
  animation-direction: reverse;
}

@keyframes scroll-left {
  0% {
    transform: translateX(0%);
  }
  100% {
    transform: translateX(-100%);
  }
}

/* Gradient overlay all'inizio del container */
.swiper-container:before {
    left: 0;
    right: auto;
    background-image: linear-gradient(90deg, rgb(18 19 22 / 85%) 80%, transparent);
    content: "";
    position: absolute;
    top: 0;
    width: 30vw;
    height: 100%;
    z-index: 2;
    background-repeat: no-repeat;
    background-position: left;
    background-size: 100% 100%;
    transition: all .25s linear;
  }

.swiper-container:hover:before {
  background-size: 0% 100%;
}

/* Animazione dello swiper */
.swiper-container .marquee-slider .swiper-wrapper {
  animation: scroll-left 20s linear infinite;
  display: flex;
  align-items: center;
  justify-content: space-around;
  min-width: 100%;
}

@media (max-width: 768px) {
  .swiper-container {
    width: 100vw;
  }

  .marquee-section {
    padding-top: 24px;
    padding-bottom: 24px;
  }

  .swiper-container:before {
    width: 40vw;
  }
}
