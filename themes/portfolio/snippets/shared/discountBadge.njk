{% set id=uid() %}

<style>
  .sellix-discount-icon-{{ id }} {
    position: absolute;
    top: 1rem;
    right: 1rem;
    pointer-events: visible;
    z-index: 1;
    display: flex;
    border-radius: 0px 20px 0px 10px;
    border: 1px solid var(--buttonColor);
    background: var(--buttonColor);
    color: var(--white)!important;
    justify-content: center;
    align-items: center;
    width: 2rem;
    height: 2rem;
    opacity: 1;
    transition: all 0.3s ease;
    font-size: 1rem;
  }

  .sellix-discount-icon-{{ id }}.group {
    position: static;
    margin-right: 0.5rem;
  }
</style>

{% if args.product.price_discount %}
  <div class="sellix-discount-icon-{{ id}} unselectable {{'group' if args.isGroup else ''}}">
    <i class="fa-regular fa-tag"></i>
  </div>
{% endif %}
